model Competitor {
	@@map("competitor")

	id          	Int      @id @default(autoincrement())
	createdAt   	DateTime @default(now())
	updatedAt   	DateTime @updatedAt()


	active		Boolean?
	telegramId		String?
	telegramFirstName		String?
	telegramLastName		String?
	username		String?
	password		String?
	idToken		String?
	accessToken		String?
	refreshToken		String?
	email		String?
	contextGroup		String?
	sQuizValutation		String?

}

